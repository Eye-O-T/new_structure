#!/bin/sh
# 패키지 제거 전에 서비스를 멈추고 부팅 자동 시작을 해제한다.
# 녹화·상태·사용자 설정을 직접 삭제하지 않아 운영 데이터가 제거 과정에서 사라지지 않게 한다.
set -eu

if [ "${1:-}" = "remove" ]; then
    systemctl disable --now ai-cctv-edge.service \
        ai-cctv-edge-control.service ai-cctv-edge-recovery.service || true
fi
