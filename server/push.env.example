# Copy alongside the Configurator-generated .env / compose.env as push.env.
# Kept separate so reinitializing the central installation preserves push setup.
PUSH_ENABLED=true
FIREBASE_PROJECT_ID=your-existing-firebase-project-id
# Absolute path, readable by the External container user. Never commit this JSON.
FIREBASE_SERVICE_ACCOUNT_FILE='C:/ProgramData/AI_CCTV/secrets/firebase-service-account.json'
