# Inference Service only. Must match DATA_INFERENCE_TOKEN in data.env.
DATA_INFERENCE_TOKEN=replace-with-a-random-inference-data-token
# Dedicated MediaMTX reader shared only with external.env.
MEDIA_READ_USERNAME=inference-reader
MEDIA_READ_PASSWORD=replace-with-at-least-32-random-characters
