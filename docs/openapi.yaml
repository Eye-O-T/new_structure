# 자동 생성: python server/services/external/tools/export_openapi.py (직접 수정하지 않음)
openapi: 3.1.0
info:
  title: AI CCTV 공개 API — 모바일 교체 계약
  version: 0.3.0
  description: |-
    모바일 등 외부 클라이언트를 교체할 때 유지해야 할 공개 HTTP API이다.
    앱은 중앙 서버의 HTTPS 주소(도메인과 포트)에 접속한다. API 경로는 /api/v1로 시작하고,
    실시간·중앙 녹화 영상은 /hls·/playback을 사용한다. Data·Edge·MediaMTX의 내부 포트에는
    직접 연결하지 않는다. 이 파일을 OpenAPI 3.1을 지원하는 뷰어에서 열면 요청·응답 형식을 볼 수 있다.

    ### 로그인과 권한
    1. POST /api/v1/auth/login으로 access_token·refresh_token과 user를 받는다.
    2. API에는 Authorization: Bearer <access_token> 또는 Access Cookie를 보낸다.
       두 가지가 함께 있으면 Bearer가 우선한다. admin은 전체, viewer는 허용 카메라만 조회한다.
    3. 401이면 현재 refresh_token으로 한 번 갱신하고 원래 요청을 재시도한다.
       갱신은 새 access·refresh 쌍을 반환하며 이전 refresh는 재사용할 수 없다.
       앱은 동시 갱신을 하나로 합치고 최신 두 토큰을 함께 저장한다. 갱신 실패는 재로그인한다.
    4. 로그아웃에는 현재 access와 refresh를 함께 보낸다. 서버는 제출된 두 토큰을 폐기하고
       refresh 계열의 기기 등록·대기 알림과 쿠키를 삭제한다. 다른 access까지 전부 즉시 철회하는
       전체 기기 로그아웃 API는 아니다. 이미 회전된 refresh 대신 반드시 최신값을 보낸다.
    기본 쿠키명은 ai_cctv_access, ai_cctv_refresh이며 배포 설정으로 바뀔 수 있다.
    로그인·갱신은 HttpOnly 쿠키도 설정한다. 기본 Secure=true, SameSite=lax,
    access Path=/, refresh Path=/api/v1/auth, 기본 수명은 각각 900초·604800초이다.
    실제 access 수명은 expires_in을 따른다. 토큰을 URL·로그에 기록하지 않는다.

    ### 영상 재생: Nginx 경로
    GET /api/v1/cameras/{camera_id}/live의 url 또는
    GET /api/v1/recordings/{segment_id}/playback의 playback_url을 그대로 사용한다.
    PUBLIC_BASE_URL 설정 시 HTTPS 절대 URL, 없으면 중앙 서버 주소 기준 상대 URL이다.
    라이브 기본 주소는 /hls/{camera_id}/index.m3u8이고 재생목록과 영상 조각을 받는 HLS이다.
    중앙 녹화 기본 주소는 /playback/get?path={camera_id}&start={UTC}&duration={초}&format=fmp4다.
    이 두 미디어 경로는 Nginx가 MediaMTX에 중계하며 FastAPI paths에 포함되지 않는다.
    auth.method=cookie는 기본 플레이어 안내다. 네이티브 플레이어의 Bearer도 Nginx가 허용하지만
    최초 재생목록뿐 아니라 모든 하위 재생목록·영상 조각·재생 요청에 헤더를 보내야 한다.
    쿠키 방식도 영상 HTTP 클라이언트와 쿠키를 공유해야 한다. Nginx는 요청마다 로그인과
    카메라 권한을 확인한다. 재생 중 401이면 토큰 갱신 후 플레이어를 다시 연다.
    mpegts 녹화는 /api/v1/recordings/{segment_id}/content를 사용하며 video/mp2t다.
    모든 녹화를 HLS로 가정하지 않는다. /content는 Range·If-Range, 200·206·416을 지원한다.
    Nginx /playback도 Range·If-Range를 전달하지만 실제 형식·탐색 지원은 MediaMTX 응답에 따른다.
    미디어 및 프록시 오류는 JSON이 아닐 수 있으므로 HTTP 상태와 Content-Type을 먼저 확인한다.

    ### 시간·목록·사람 정보
    from·to는 시간대가 포함된 RFC 3339이며 함께 지정하면 from < to다.
    이벤트는 from 이상·to 미만, 녹화는 요청 구간과 겹치는 항목을 반환한다.
    서버는 UTC로 정규화하며 화면에서 현지 시각으로 바꾼다. 목록은 limit(기본 50, 최대 100),
    offset을 사용한다. items가 빌 때까지 offset을 증가시키며 총 개수 필드를 가정하지 않는다.
    person_id는 (camera_id, tracking_session_id) 안에서만 유효한 추적 번호다.
    global_person_id는 여러 카메라의 인물을 연결하는 별도 ID이며 미판정 시 null이다.
    confidence는 감지기의 확신 정도(0~1)이며 신원 일치 확률이 아니다.
    이벤트 metadata는 확장 가능한 객체다. identity와 analysis 결과는 이벤트 생성 후 갱신되며
    기본 블랙박스는 unconfigured다. 알 수 없는 이벤트 유형·metadata 키도 허용한다.
    objects는 최신 좌표 조회 API이며 영상 프레임과 정확히 동기화된 추적 스트림이 아니다.
    관측 시각·원본 크기로 표시 여부와 좌표를 계산하고 stale=true이면 박스를 지운다.
    snapshot_path 등 저장소 상대 경로를 공개 이미지 URL로 조합하지 않는다. 별도 스냅샷 API는 없다.

    ### FCM 수신 계약
    로그인 후 현재 refresh_token과 함께 PUT /api/v1/notifications/devices로 기기를 등록한다.
    event_types=null은 모든 이벤트, []는 수신 안 함, 문자열 목록은 지정 유형이다.
    refresh 회전은 기존 기기 등록을 유지한다. 등록 요청이 401이면 갱신 후 Body도 최신 refresh로 바꾼다.
    FCM 기기 토큰이 변경되면 새 token으로 PUT 요청을 다시 보내 등록을 갱신한다.
    기기 ID는 소문자 16진수 32자리다. 새 로그인마다 새 ID를 사용하면 이전 계정의 지연 알림을 구별할 수 있다.
    FCM data는 문자열 event_id, user_id, device_id, camera_id, occurred_at을 포함한다.
    Android 앱은 알림 권한을 요청하고 cctv_events 채널을 생성한다.
    메시지는 notification과 data를 함께 포함한다. 앱 실행 중에는 앱이 알림을 표시하고,
    백그라운드에서는 OS가 표시한 알림을 다시 생성하지 않아 중복을 막는다.
    알림 탭은 백그라운드 복귀와 앱 종료 후 시작 모두 처리하고, 로그인 초기화 후 상세를 연다.
    알림에는 일반 안내만 있으며 영상·JWT·비밀번호는 없다.
    중복·지연 수신에 대비해 event_id로 중복 표시를 줄이고 현재 사용자·기기 ID를 확인한 뒤
    GET /api/v1/events/{event_id}로 내용을 다시 조회한다. 권한 회수·로그아웃 뒤 도착한 알림을 그대로 신뢰하지 않는다.
    발송은 이벤트 생성 시 등록된 수신자만 대상으로 하며 기존 이벤트를 새 기기에 소급 발송하지 않는다.
    최대 8회·생성 후 1시간 범위에서 재시도한다. FCM 접수와 단말 도착은 서로 다르다.
    Firebase 서버·Android 설정과 실기기 확인이 필요하다.
    GET /api/v1/notifications/status는 서버의 푸시 사용 설정만 나타낸다.

    ### 오류와 명세 한계
    일반 오류는 {detail: 문자열}, 422는 {detail: [{type, loc, msg}]}이다.
    제어 오류와 일부 충돌은 {error: {code, message, details}}다. 400·422는 입력 수정,
    403·404는 권한·자원 확인, 409는 상태 충돌 확인, 429는 Retry-After 이후 재시도한다.
    502·503·504에는 제한된 지수 지연을 적용하되 변경 요청의 적용 여부를 먼저 조회한다.
    주요 코드: EDGE_OFFLINE, CAPABILITY_UNKNOWN, UNSUPPORTED_VIDEO_PROFILE, CONTROL_TIMEOUT,
    MEDIA_CONTROL_UNAVAILABLE, CAMERA_HAS_HISTORY, CAMERA_LIMIT_REACHED. 알 수 없는 코드도 처리한다.
    metadata·시스템 상태의 data 내부 필드는 자유 형식이다. 모든 장애 코드나 MediaMTX 미디어 형식을
    열거한 명세는 아니다. 이 파일은 실행 서버의 /api/v1/openapi.json에 사용 설명과 응답 형식을
    보완해 생성한 문서다. 생성·일치 검사는 파일 끝의 x-generation 명령을 사용한다.
paths:
  /api/v1/notifications/status:
    get:
      tags:
      - notifications
      summary: 푸시 설정 여부
      operationId: push_status_api_v1_notifications_status_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PushStatus'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      security:
      - HTTPBearer: []
      - AccessCookie: []
      x-schema-source: 실제 반환 코드를 기반으로 문서 생성기가 보완한 응답
      description: enabled는 서버의 PUSH_ENABLED 설정이다. Firebase 연결 성공이나 단말 도착을 확인하지 않는다.
  /api/v1/notifications/devices:
    put:
      tags:
      - notifications
      summary: 알림 기기 등록·수신 설정
      operationId: register_device_api_v1_notifications_devices_put
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeviceRegistration'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeviceRegistrationResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      security:
      - HTTPBearer: []
      - AccessCookie: []
      x-schema-source: 실제 반환 코드를 기반으로 문서 생성기가 보완한 응답
      description: 현재 사용자 소유의 유효한 refresh_token이 필요하다. user_id·역할은 서버가 정한다. event_types=null은 전체, []는
        없음이다. 토큰 문자열에는 공백이 없어야 한다.
  /api/v1/notifications/devices/{device_id}:
    delete:
      tags:
      - notifications
      summary: 알림 기기 등록 해제
      operationId: unregister_device_api_v1_notifications_devices__device_id__delete
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: device_id
        in: path
        required: true
        schema:
          type: string
          pattern: ^[a-f0-9]{32}$
          title: Device Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 현재 사용자 소유 기기만 해제한다. 없는 등록도 204다. 로그아웃은 현재 refresh를 함께 보내 세션까지 철회한다.
  /api/v1/admin/system/status:
    get:
      summary: 시스템 상태 조회
      operationId: system_status_api_v1_admin_system_status_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStatusResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      security:
      - HTTPBearer: []
      - AccessCookie: []
      description: 관리자 전용. External과 Data 상태를 반환한다. 여섯 컨테이너 전체·영상·FCM 수신의 성공 판정은 아니다. /system/status와
        /admin/system/status는 같은 기능이다.
      tags:
      - admin
  /api/v1/system/status:
    get:
      summary: 시스템 상태 조회
      operationId: system_status_api_v1_system_status_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemStatusResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      security:
      - HTTPBearer: []
      - AccessCookie: []
      description: 관리자 전용. External과 Data 상태를 반환한다. 여섯 컨테이너 전체·영상·FCM 수신의 성공 판정은 아니다. /system/status와
        /admin/system/status는 같은 기능이다.
      tags:
      - system
  /api/v1/auth/login:
    post:
      summary: 로그인
      operationId: login_api_v1_auth_login_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          headers:
            Set-Cookie:
              description: access·refresh 쿠키를 각각 설정한다. 수명·경로·기본 이름은 위 인증 설명 참조.
              schema:
                type: string
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        default:
          $ref: '#/components/responses/ApiError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          description: 반복 로그인 실패로 일시 지연됨
          headers:
            Retry-After:
              description: 다시 시도하기 전 기다릴 초
              schema:
                type: integer
                minimum: 1
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DetailError'
      description: 계정 확인 후 토큰 쌍과 쿠키를 발급한다. 반복 실패 시 429와 Retry-After를 반환한다.
      tags:
      - auth
      security: []
  /api/v1/auth/refresh:
    post:
      summary: 토큰 갱신
      operationId: refresh_api_v1_auth_refresh_post
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/RefreshRequest'
              - type: 'null'
              title: Payload
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
          headers:
            Set-Cookie:
              description: access·refresh 쿠키를 각각 설정한다. 수명·경로·기본 이름은 위 인증 설명 참조.
              schema:
                type: string
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        default:
          $ref: '#/components/responses/ApiError'
        '401':
          $ref: '#/components/responses/Unauthorized'
      description: Body의 refresh_token이 쿠키보다 우선한다. 둘 중 하나가 필요하다. 성공하면 이전 refresh를 폐기하므로 동시 갱신을 피한다.
      tags:
      - auth
      security: []
      parameters:
      - in: cookie
        name: ai_cctv_refresh
        required: false
        schema:
          type: string
        description: Body의 refresh_token이 없을 때 사용한다. REFRESH_COOKIE_NAME 설정으로 이름이 바뀔 수 있다.
  /api/v1/auth/logout:
    post:
      summary: 로그아웃
      operationId: logout_api_v1_auth_logout_post
      requestBody:
        content:
          application/json:
            schema:
              anyOf:
              - $ref: '#/components/schemas/LogoutRequest'
              - type: 'null'
              title: Payload
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        default:
          $ref: '#/components/responses/ApiError'
      description: Body 또는 쿠키의 현재 refresh와 Authorization 또는 쿠키의 access를 함께 보낸다. 유효한 토큰만 철회하며 토큰이 없거나 만료된
        경우에도 쿠키 삭제 후 204일 수 있다. refresh 철회는 그 계열의 기기·대기 알림도 해제한다. 이미 FCM에 접수된 알림은 취소되지 않는다.
      tags:
      - auth
      security:
      - HTTPBearer: []
      - AccessCookie: []
      - {}
      parameters:
      - in: cookie
        name: ai_cctv_refresh
        required: false
        schema:
          type: string
        description: Body의 refresh_token이 없을 때 사용한다. REFRESH_COOKIE_NAME 설정으로 이름이 바뀔 수 있다.
  /api/v1/cameras:
    get:
      summary: 카메라 목록
      operationId: list_cameras_api_v1_cameras_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 50
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
        description: 건너뛸 항목 수. items가 빈 목록이면 조회를 마친다.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraPageResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 현재 사용자가 볼 수 있는 카메라를 조회한다. 비밀값과 Edge 내부 주소는 공개 응답에서 제외한다.
      tags:
      - cameras
    post:
      summary: 카메라 등록
      operationId: create_camera_api_v1_cameras_post
      security:
      - HTTPBearer: []
      - AccessCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CameraCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. stream_path는 생략하거나 camera_id와 같아야 한다. Edge 연동 시 edge_device_id, edge_management_url,
        edge_recovery_url, edge_auth_token을 모두 보낸다. 활성 카메라 최대 4개. publish_credentials 비밀번호는 이 응답에서만 받으므로
        일치하는 Edge에 안전하게 전달한다.
      tags:
      - cameras
  /api/v1/cameras/{camera_id}:
    patch:
      summary: 카메라 설정 변경
      operationId: update_camera_api_v1_cameras__camera_id__patch
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CameraPatch'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. 변경할 필드만 보낸다. enabled=false는 새 송출과 Live를 차단하고 기존 송출 연결을 끊는다. Media 제어 실패 시 차단
        상태를 유지하므로 상태를 확인하고 재시도한다.
      tags:
      - cameras
    delete:
      summary: 카메라 삭제
      operationId: delete_camera_api_v1_cameras__camera_id__delete
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. 이력이 있으면 CAMERA_HAS_HISTORY 409로 거부한다. 이력 보존이 필요하면 enabled=false를 사용한다.
      tags:
      - cameras
    get:
      summary: 카메라 조회
      operationId: get_camera_api_v1_cameras__camera_id__get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 현재 사용자에게 해당 카메라의 열람 권한이 있어야 한다.
      tags:
      - cameras
  /api/v1/cameras/{camera_id}/publish-credentials/rotate:
    post:
      summary: 송출 비밀번호 교체
      description: 관리자 전용. 기존 송출자를 끊고 비밀번호를 교체한다. 새 publish_credentials는 한 번만 반환한다. 중간 실패 시 카메라가 비활성 상태로
        남을 수 있으므로 상태를 확인한다.
      operationId: rotate_camera_publish_credentials_api_v1_cameras__camera_id__publish_credentials_rotate_post
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      tags:
      - cameras
  /api/v1/cameras/{camera_id}/live:
    get:
      summary: 실시간 영상 주소
      operationId: get_camera_live_api_v1_cameras__camera_id__live_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraLiveResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: url을 재생한다. hls_url은 같은 주소의 호환 필드다. 비활성 카메라는 409. 실제 HLS는 Nginx 경로이며 재생목록과 모든 조각에 인증이
        필요하다.
      tags:
      - cameras
  /api/v1/cameras/{camera_id}/video-profile:
    get:
      summary: 영상 설정 조회
      operationId: get_camera_video_profile_api_v1_cameras__camera_id__video_profile_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoProfileResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 현재값·요청값·지원 profile과 Edge 연결 여부를 반환한다.
      tags:
      - cameras
    patch:
      summary: 영상 설정 변경
      operationId: update_camera_video_profile_api_v1_cameras__camera_id__video_profile_patch
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VideoProfilePatch'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoProfileResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. hd=720p/30fps/2Mbps, fhd=1080p/30fps/4Mbps다. Edge 적용 후 현재값이 바뀐다. 잠금·적용·복구에
        시간이 걸리므로 약 90초의 클라이언트 대기 시간을 고려하고 오류 후 조회로 결과를 확인한다.
      tags:
      - cameras
  /api/v1/cameras/{camera_id}/status:
    get:
      summary: 카메라 장치 상태
      operationId: get_camera_status_api_v1_cameras__camera_id__status_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraStatusResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: Edge HTTP 연결, 카메라 입력, 중앙 전송 상태는 별개다. 센서가 없는 수치는 null일 수 있다.
      tags:
      - cameras
  /api/v1/cameras/{camera_id}/objects:
    get:
      summary: 최신 객체 좌표
      operationId: get_live_objects_api_v1_cameras__camera_id__objects_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: path
        required: true
        schema:
          type: string
          title: Camera Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicLiveObjects'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      x-schema-source: 실제 반환 코드를 기반으로 문서 생성기가 보완한 응답
      description: 원본 픽셀 기준 bbox=[x1,y1,x2,y2]다. 화면 비율·여백을 반영해 그린다. 최근 3초 이내 관측만 반환하고 없거나 오래됐거나 미래 시각이면
        stale=true와 빈 objects를 반환한다. stale일 때 세션·크기·관측 시각 필드는 없다. HLS 프레임과 정확히 동기화되지 않는다.
      tags:
      - cameras
  /api/v1/recordings:
    get:
      summary: 녹화 구간 검색
      operationId: list_recordings_api_v1_recordings_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: query
        required: true
        schema:
          type: string
          title: Camera Id
      - name: from
        in: query
        required: true
        schema:
          type: string
          format: date-time
          title: From
        description: '시간대를 포함한 RFC 3339. 예: 2026-09-07T00:00:00Z. 함께 지정하면 from < to.'
      - name: to
        in: query
        required: true
        schema:
          type: string
          format: date-time
          title: To
        description: '시간대를 포함한 RFC 3339. 예: 2026-09-07T00:00:00Z. 함께 지정하면 from < to.'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 50
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
        description: 건너뛸 항목 수. items가 빈 목록이면 조회를 마친다.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordingPageResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: camera_id·from·to가 필요하다. 요청 시간 구간과 겹치는 녹화를 반환한다. 권한이 있는 카메라만 조회한다.
      tags:
      - recordings
  /api/v1/recordings/{segment_id}:
    get:
      summary: 녹화 구간 조회
      operationId: get_recording_api_v1_recordings__segment_id__get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: segment_id
        in: path
        required: true
        schema:
          type: string
          title: Segment Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordingResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 녹화 정보와 그 카메라의 열람 권한을 확인한다.
      tags:
      - recordings
  /api/v1/recordings/{segment_id}/playback:
    get:
      summary: 녹화 재생 주소
      operationId: get_recording_playback_api_v1_recordings__segment_id__playback_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: segment_id
        in: path
        required: true
        schema:
          type: string
          title: Segment Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecordingPlaybackResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: format=mpegts면 공개 /content 주소, 그 외에는 MediaMTX fmp4 Playback 주소를 반환한다. 주소를 직접 조합하거나
        모두 HLS라고 가정하지 않는다.
      tags:
      - recordings
  /api/v1/recordings/{segment_id}/content:
    get:
      summary: 녹화 영상 바이트
      operationId: get_recording_content_api_v1_recordings__segment_id__content_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: segment_id
        in: path
        required: true
        schema:
          type: string
          title: Segment Id
      - name: Range
        in: header
        required: false
        description: '단일 Byte Range. 예: bytes=0-1023, bytes=1024-, bytes=-1024'
        schema:
          type: string
      - name: If-Range
        in: header
        required: false
        description: 이전 응답의 ETag 또는 HTTP 날짜. 일치하지 않으면 전체 200.
        schema:
          type: string
      responses:
        '200':
          description: Complete recording content
          content:
            video/mp2t:
              schema:
                type: string
                format: binary
            video/mp4:
              schema:
                type: string
                format: binary
          headers:
            Accept-Ranges:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Content-Range:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            ETag:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Last-Modified:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Content-Length:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
        '206':
          description: Recording byte range
          content:
            video/mp2t:
              schema:
                type: string
                format: binary
            video/mp4:
              schema:
                type: string
                format: binary
          headers:
            Accept-Ranges:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Content-Range:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            ETag:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Last-Modified:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Content-Length:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
        '416':
          description: Unsatisfiable byte range
          headers:
            Accept-Ranges:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Content-Range:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            ETag:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Last-Modified:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
            Content-Length:
              schema:
                type: string
              description: 상태와 요청에 따라 제공되는 원본 영상 응답 헤더
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: Range가 없으면 전체 200, 유효한 단일 Byte Range는 206, 범위를 만족할 수 없으면 416이다. If-Range의 ETag·시각이
        현재 파일과 다르면 전체 200을 반환한다.
      tags:
      - recordings
  /api/v1/recovery-jobs:
    get:
      summary: 영상 복구 작업 목록
      operationId: list_recovery_jobs_api_v1_recovery_jobs_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Camera Id
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 50
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
        description: 건너뛸 항목 수. items가 빈 목록이면 조회를 마친다.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecoveryJobPageResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. Edge 장애 구간의 복구 진행 상태와 오류를 조회한다.
      tags:
      - recovery-jobs
  /api/v1/events:
    get:
      summary: 이벤트 검색
      operationId: list_events_api_v1_events_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: camera_id
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          title: Camera Id
      - name: event_type
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
            maxLength: 128
          - type: 'null'
          title: Event Type
      - name: from
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: From
        description: '시간대를 포함한 RFC 3339. 예: 2026-09-07T00:00:00Z. 함께 지정하면 from < to.'
      - name: to
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: To
        description: '시간대를 포함한 RFC 3339. 예: 2026-09-07T00:00:00Z. 함께 지정하면 from < to.'
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 50
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
        description: 건너뛸 항목 수. items가 빈 목록이면 조회를 마친다.
      - name: cursor
        in: query
        required: false
        schema:
          anyOf:
          - type: string
            minLength: 1
            maxLength: 1024
          - type: 'null'
          title: Cursor
      - name: order
        in: query
        required: false
        schema:
          enum:
          - asc
          - desc
          type: string
          default: asc
          title: Order
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventPageResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: viewer는 camera_id를 반드시 지정해야 한다. admin은 생략하여 전체를 검색할 수 있다. event_type·from·to는 선택이며
        시각 범위는 from 이상·to 미만이다. 인물 연결·분석 결과는 나중에 갱신될 수 있다.
      tags:
      - events
  /api/v1/events/{event_id}:
    get:
      summary: 이벤트 상세
      operationId: get_event_api_v1_events__event_id__get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: event_id
        in: path
        required: true
        schema:
          type: string
          title: Event Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 푸시의 event_id로도 조회한다. snapshot_path는 공개 URL이 아니다. 연결 녹화는 recording_segment_id 또는 recording_segment_ids로
        조회한다.
      tags:
      - events
  /api/v1/admin/users:
    get:
      summary: 사용자 목록
      operationId: list_users_api_v1_admin_users_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 1
          default: 50
          title: Limit
      - name: offset
        in: query
        required: false
        schema:
          type: integer
          minimum: 0
          default: 0
          title: Offset
        description: 건너뛸 항목 수. items가 빈 목록이면 조회를 마친다.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPageResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. 사용자 계정과 역할을 조회한다. 비밀번호 해시는 반환하지 않는다.
      tags:
      - admin
    post:
      summary: 사용자 생성
      operationId: create_user_api_v1_admin_users_post
      security:
      - HTTPBearer: []
      - AccessCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserCreate'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. admin 또는 viewer 역할을 지정한다. 새 비밀번호는 12자 이상이어야 한다.
      tags:
      - admin
  /api/v1/admin/users/{user_id}:
    patch:
      summary: 사용자 변경
      operationId: update_user_api_v1_admin_users__user_id__patch
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: string
          title: User Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserPatch'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. 비밀번호·역할·활성 상태 중 변경할 필드만 보낸다. 역할 변경 후 기존 access는 사용할 수 없으므로 토큰 갱신 또는 재로그인이 필요하다.
      tags:
      - admin
  /api/v1/admin/users/{user_id}/camera-permissions:
    get:
      summary: 사용자 카메라 권한 조회
      operationId: get_user_permissions_api_v1_admin_users__user_id__camera_permissions_get
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: string
          title: User Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraPermissionListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. 사용자에게 배정된 카메라 목록을 반환한다.
      tags:
      - admin
    put:
      summary: 사용자 카메라 권한 교체
      operationId: set_user_permissions_api_v1_admin_users__user_id__camera_permissions_put
      security:
      - HTTPBearer: []
      - AccessCookie: []
      parameters:
      - name: user_id
        in: path
        required: true
        schema:
          type: string
          title: User Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CameraPermissions'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CameraPermissionListResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
        '401':
          $ref: '#/components/responses/Unauthorized'
        default:
          $ref: '#/components/responses/ApiError'
      description: 관리자 전용. camera_ids 전체 목록으로 현재 배정을 교체한다. 빈 목록은 모든 배정을 해제한다.
      tags:
      - admin
components:
  schemas:
    CameraCreate:
      properties:
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        name:
          type: string
          maxLength: 256
          minLength: 1
          title: Name
        stream_path:
          anyOf:
          - type: string
            pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          - type: 'null'
          title: Stream Path
        source_url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Source Url
        edge_device_id:
          anyOf:
          - type: string
            maxLength: 256
            minLength: 1
          - type: 'null'
          title: Edge Device Id
        edge_management_url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Edge Management Url
        edge_recovery_url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Edge Recovery Url
        edge_auth_token:
          anyOf:
          - type: string
            maxLength: 4096
            minLength: 32
            format: password
            writeOnly: true
          - type: 'null'
          title: Edge Auth Token
        enabled:
          type: boolean
          title: Enabled
          default: true
      additionalProperties: false
      type: object
      required:
      - camera_id
      - name
      title: CameraCreate
    CameraLiveResponse:
      properties:
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        protocol:
          type: string
          const: hls
          title: Protocol
        url:
          type: string
          title: Url
        hls_url:
          type: string
          title: Hls Url
        auth:
          $ref: '#/components/schemas/LiveAuthResponse'
      type: object
      required:
      - camera_id
      - protocol
      - url
      - hls_url
      - auth
      title: CameraLiveResponse
    CameraPageResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/CameraResponse'
          type: array
          title: Items
        limit:
          type: integer
          minimum: 1.0
          title: Limit
        offset:
          type: integer
          minimum: 0.0
          title: Offset
      type: object
      required:
      - items
      - limit
      - offset
      title: CameraPageResponse
    CameraPatch:
      properties:
        name:
          anyOf:
          - type: string
            maxLength: 128
            minLength: 1
          - type: 'null'
          title: Name
        stream_path:
          anyOf:
          - type: string
            pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          - type: 'null'
          title: Stream Path
        source_url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Source Url
        edge_device_id:
          anyOf:
          - type: string
            maxLength: 256
            minLength: 1
          - type: 'null'
          title: Edge Device Id
        edge_management_url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Edge Management Url
        edge_recovery_url:
          anyOf:
          - type: string
            maxLength: 2048
          - type: 'null'
          title: Edge Recovery Url
        edge_auth_token:
          anyOf:
          - type: string
            maxLength: 4096
            minLength: 32
            format: password
            writeOnly: true
          - type: 'null'
          title: Edge Auth Token
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
      additionalProperties: false
      type: object
      title: CameraPatch
    CameraPermissionListResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/CameraResponse'
          type: array
          title: Items
      type: object
      required:
      - items
      title: CameraPermissionListResponse
    CameraPermissions:
      properties:
        camera_ids:
          items:
            type: string
          type: array
          maxItems: 256
          title: Camera Ids
      additionalProperties: false
      type: object
      required:
      - camera_ids
      title: CameraPermissions
    CameraResponse:
      properties:
        id:
          anyOf:
          - type: integer
          - type: 'null'
          title: Id
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        name:
          anyOf:
          - type: string
          - type: 'null'
          title: Name
        stream_path:
          anyOf:
          - type: string
          - type: 'null'
          title: Stream Path
        enabled:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Enabled
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
        publish_credentials:
          anyOf:
          - $ref: '#/components/schemas/PublishCredentialsResponse'
          - type: 'null'
      type: object
      required:
      - camera_id
      title: CameraResponse
    CameraStatusResponse:
      properties:
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        online:
          type: boolean
          title: Online
        cpu_percent:
          anyOf:
          - type: number
          - type: 'null'
          title: Cpu Percent
        memory_percent:
          anyOf:
          - type: number
          - type: 'null'
          title: Memory Percent
        storage_percent:
          anyOf:
          - type: number
          - type: 'null'
          title: Storage Percent
        battery_percent:
          anyOf:
          - type: number
          - type: 'null'
          title: Battery Percent
        power_source:
          type: string
          enum:
          - external
          - battery
          - unknown
          title: Power Source
        camera_input:
          type: string
          enum:
          - online
          - offline
          - lost
          - unknown
          title: Camera Input
        central_connection_status:
          type: string
          enum:
          - online
          - offline
          - unknown
          title: Central Connection Status
        current_video_profile:
          type: string
          enum:
          - hd
          - fhd
          title: Current Video Profile
        last_seen_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Last Seen At
        last_error_code:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error Code
      type: object
      required:
      - camera_id
      - online
      - cpu_percent
      - memory_percent
      - storage_percent
      - battery_percent
      - power_source
      - camera_input
      - central_connection_status
      - current_video_profile
      - last_seen_at
      - last_error_code
      title: CameraStatusResponse
    DeviceRegistration:
      properties:
        device_id:
          type: string
          pattern: ^[a-f0-9]{32}$
          title: Device Id
        token:
          type: string
          maxLength: 4096
          minLength: 20
          format: password
          title: Token
          writeOnly: true
        refresh_token:
          type: string
          maxLength: 8192
          minLength: 1
          format: password
          title: Refresh Token
          writeOnly: true
          description: 현재 로그인한 사용자 소유의 아직 회전되지 않은 refresh 토큰
        platform:
          type: string
          enum:
          - android
          - ios
          title: Platform
        enabled:
          type: boolean
          title: Enabled
        event_types:
          anyOf:
          - items:
              type: string
            type: array
            maxItems: 100
          - type: 'null'
          title: Event Types
          description: null=모든 이벤트, []=수신 없음. 각 항목은 ^[a-z][a-z0-9_]{0,127}$이며 중복 제거·정렬 후 저장한다.
      additionalProperties: false
      type: object
      required:
      - device_id
      - token
      - refresh_token
      - platform
      - enabled
      - event_types
      title: DeviceRegistration
    EventPageResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/EventResponse'
          type: array
          title: Items
        limit:
          anyOf:
          - type: integer
            minimum: 1.0
          - type: 'null'
          title: Limit
        offset:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Offset
        snapshot_max_id:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Snapshot Max Id
        next_cursor:
          anyOf:
          - type: string
          - type: 'null'
          title: Next Cursor
        has_more:
          type: boolean
          title: Has More
          default: false
      type: object
      required:
      - items
      title: EventPageResponse
    EventResponse:
      properties:
        id:
          anyOf:
          - type: integer
          - type: string
          title: Id
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        event_type:
          type: string
          title: Event Type
        occurred_at:
          type: string
          format: date-time
          title: Occurred At
        person_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Person Id
        global_person_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Global Person Id
        confidence:
          anyOf:
          - type: number
            maximum: 1.0
            minimum: 0.0
          - type: 'null'
          title: Confidence
        recording_segment_id:
          anyOf:
          - type: integer
          - type: 'null'
          title: Recording Segment Id
        recording_segment_ids:
          items:
            type: integer
          type: array
          title: Recording Segment Ids
        snapshot_path:
          anyOf:
          - type: string
          - type: 'null'
          title: Snapshot Path
        metadata:
          type: object
          title: Metadata
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
      type: object
      required:
      - id
      - camera_id
      - event_type
      - occurred_at
      title: EventResponse
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    LiveAuthResponse:
      properties:
        method:
          type: string
          const: cookie
          title: Method
        cookie_name:
          type: string
          title: Cookie Name
      type: object
      required:
      - method
      - cookie_name
      title: LiveAuthResponse
    LoginRequest:
      properties:
        username:
          type: string
          maxLength: 128
          minLength: 1
          pattern: ^[^/\\\x00-\x1f]+$
          title: Username
        password:
          type: string
          maxLength: 1024
          minLength: 1
          format: password
          title: Password
          writeOnly: true
      additionalProperties: false
      type: object
      required:
      - username
      - password
      title: LoginRequest
    LogoutRequest:
      properties:
        refresh_token:
          anyOf:
          - type: string
            format: password
            writeOnly: true
          - type: 'null'
          title: Refresh Token
      additionalProperties: false
      type: object
      title: LogoutRequest
    PublishCredentialsResponse:
      properties:
        username:
          type: string
          title: Username
        password:
          type: string
          title: Password
      type: object
      required:
      - username
      - password
      title: PublishCredentialsResponse
    RecordingPageResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/RecordingResponse'
          type: array
          title: Items
        limit:
          anyOf:
          - type: integer
            minimum: 1.0
          - type: 'null'
          title: Limit
        offset:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Offset
      type: object
      required:
      - items
      title: RecordingPageResponse
    RecordingPlaybackResponse:
      properties:
        recording_id:
          type: string
          title: Recording Id
        playback_url:
          type: string
          title: Playback Url
      type: object
      required:
      - recording_id
      - playback_url
      title: RecordingPlaybackResponse
    RecordingResponse:
      properties:
        id:
          anyOf:
          - type: integer
          - type: string
          title: Id
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        start_time:
          type: string
          format: date-time
          title: Start Time
        end_time:
          type: string
          format: date-time
          title: End Time
        format:
          anyOf:
          - type: string
            enum:
            - fmp4
            - mp4
            - mpegts
          - type: 'null'
          title: Format
        codec:
          anyOf:
          - type: string
          - type: 'null'
          title: Codec
        duration_ms:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Duration Ms
        file_size:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: File Size
        source:
          anyOf:
          - type: string
            enum:
            - central
            - edge_recovery
            - import
          - type: 'null'
          title: Source
        status:
          anyOf:
          - type: string
            enum:
            - writing
            - ready
            - missing
            - corrupt
            - deleting
            - deleted
          - type: 'null'
          title: Status
        checksum:
          anyOf:
          - type: string
          - type: 'null'
          title: Checksum
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
      type: object
      required:
      - id
      - camera_id
      - start_time
      - end_time
      title: RecordingResponse
    RecoveryJobPageResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/RecoveryJobResponse'
          type: array
          title: Items
        limit:
          anyOf:
          - type: integer
            minimum: 1.0
          - type: 'null'
          title: Limit
        offset:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Offset
      type: object
      required:
      - items
      title: RecoveryJobPageResponse
    RecoveryJobResponse:
      properties:
        id:
          type: integer
          title: Id
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        outage_started_at:
          type: string
          format: date-time
          title: Outage Started At
        outage_ended_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Outage Ended At
        status:
          type: string
          enum:
          - detected
          - waiting_for_recovery
          - downloading
          - indexing
          - completed
          - failed
          title: Status
        attempt_count:
          type: integer
          minimum: 0.0
          title: Attempt Count
        max_attempts:
          type: integer
          exclusiveMinimum: 0.0
          title: Max Attempts
        revision:
          type: integer
          minimum: 0.0
          title: Revision
          default: 0
        next_retry_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Next Retry At
        last_error:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error
        recovery_summary:
          anyOf:
          - type: object
          - type: 'null'
          title: Recovery Summary
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
      type: object
      required:
      - id
      - camera_id
      - outage_started_at
      - outage_ended_at
      - status
      - attempt_count
      - max_attempts
      title: RecoveryJobResponse
    RefreshRequest:
      properties:
        refresh_token:
          anyOf:
          - type: string
            format: password
            writeOnly: true
          - type: 'null'
          title: Refresh Token
      additionalProperties: false
      type: object
      title: RefreshRequest
    ServiceStatusResponse:
      properties:
        status:
          type: string
          title: Status
        version:
          anyOf:
          - type: string
          - type: 'null'
          title: Version
      type: object
      required:
      - status
      title: ServiceStatusResponse
    SystemStatusResponse:
      properties:
        status:
          type: string
          enum:
          - ready
          - degraded
          title: Status
        external:
          $ref: '#/components/schemas/ServiceStatusResponse'
        data:
          type: object
          title: Data
        preprocessing:
          type: object
          title: Preprocessing
        media:
          type: object
          title: Media
        push:
          type: object
          title: Push
      type: object
      required:
      - status
      - external
      - data
      - preprocessing
      - media
      - push
      title: SystemStatusResponse
    TokenResponse:
      properties:
        access_token:
          type: string
          title: Access Token
        refresh_token:
          type: string
          title: Refresh Token
        token_type:
          type: string
          const: bearer
          title: Token Type
        expires_in:
          type: integer
          exclusiveMinimum: 0.0
          title: Expires In
        user:
          $ref: '#/components/schemas/UserResponse'
      type: object
      required:
      - access_token
      - refresh_token
      - token_type
      - expires_in
      - user
      title: TokenResponse
    UserCreate:
      properties:
        username:
          type: string
          maxLength: 128
          minLength: 1
          pattern: ^[^/\\\x00-\x1f]+$
          title: Username
        password:
          type: string
          maxLength: 1024
          minLength: 12
          format: password
          title: Password
          writeOnly: true
        role:
          type: string
          enum:
          - admin
          - viewer
          title: Role
        is_active:
          type: boolean
          title: Is Active
          default: true
      additionalProperties: false
      type: object
      required:
      - username
      - password
      - role
      title: UserCreate
    UserPageResponse:
      properties:
        items:
          items:
            $ref: '#/components/schemas/UserResponse'
          type: array
          title: Items
        limit:
          anyOf:
          - type: integer
            minimum: 1.0
          - type: 'null'
          title: Limit
        offset:
          anyOf:
          - type: integer
            minimum: 0.0
          - type: 'null'
          title: Offset
      type: object
      required:
      - items
      title: UserPageResponse
    UserPatch:
      properties:
        password:
          anyOf:
          - type: string
            maxLength: 1024
            minLength: 12
            format: password
            writeOnly: true
          - type: 'null'
          title: Password
        role:
          anyOf:
          - type: string
            enum:
            - admin
            - viewer
          - type: 'null'
          title: Role
        is_active:
          anyOf:
          - type: boolean
          - type: 'null'
          title: Is Active
      additionalProperties: false
      type: object
      title: UserPatch
    UserResponse:
      properties:
        id:
          anyOf:
          - type: integer
          - type: string
          title: Id
        username:
          type: string
          title: Username
        role:
          type: string
          enum:
          - admin
          - viewer
          title: Role
        is_active:
          type: boolean
          title: Is Active
        created_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Created At
        updated_at:
          anyOf:
          - type: string
            format: date-time
          - type: 'null'
          title: Updated At
      type: object
      required:
      - id
      - username
      - role
      - is_active
      title: UserResponse
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
    VideoProfilePatch:
      properties:
        profile:
          type: string
          enum:
          - hd
          - fhd
          title: Profile
      additionalProperties: false
      type: object
      required:
      - profile
      title: VideoProfilePatch
    VideoProfileResponse:
      properties:
        camera_id:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
          title: Camera Id
        current_profile:
          type: string
          enum:
          - hd
          - fhd
          title: Current Profile
        desired_profile:
          type: string
          enum:
          - hd
          - fhd
          title: Desired Profile
        supported_profiles:
          items:
            type: string
            enum:
            - hd
            - fhd
          type: array
          minItems: 1
          title: Supported Profiles
        edge_online:
          type: boolean
          title: Edge Online
        last_error_code:
          anyOf:
          - type: string
          - type: 'null'
          title: Last Error Code
      type: object
      required:
      - camera_id
      - current_profile
      - desired_profile
      - supported_profiles
      - edge_online
      - last_error_code
      title: VideoProfileResponse
    PublicLiveObject:
      additionalProperties: false
      properties:
        person_id:
          maxLength: 256
          minLength: 1
          title: Person Id
          type: string
        bbox:
          maxItems: 4
          minItems: 4
          prefixItems:
          - type: integer
          - type: integer
          - type: integer
          - type: integer
          title: Bbox
          type: array
        confidence:
          maximum: 1.0
          minimum: 0.0
          title: Confidence
          type: number
        global_person_id:
          anyOf:
          - type: string
          - type: 'null'
          title: Global Person Id
      required:
      - person_id
      - bbox
      - confidence
      - global_person_id
      title: PublicLiveObject
      type: object
    FreshLiveObjects:
      additionalProperties: false
      properties:
        tracking_session_id:
          pattern: ^[a-f0-9]{32}$
          title: Tracking Session Id
          type: string
        observed_at:
          format: date-time
          title: Observed At
          type: string
        frame_width:
          exclusiveMinimum: 0
          maximum: 16384
          title: Frame Width
          type: integer
        frame_height:
          exclusiveMinimum: 0
          maximum: 16384
          title: Frame Height
          type: integer
        objects:
          items:
            $ref: '#/components/schemas/PublicLiveObject'
          maxItems: 100
          title: Objects
          type: array
        camera_id:
          title: Camera Id
          type: string
        stale:
          const: false
          title: Stale
          type: boolean
      required:
      - tracking_session_id
      - observed_at
      - frame_width
      - frame_height
      - objects
      - camera_id
      - stale
      title: FreshLiveObjects
      type: object
    StaleLiveObjects:
      properties:
        camera_id:
          title: Camera Id
          type: string
        stale:
          const: true
          title: Stale
          type: boolean
        objects:
          items: {}
          maxItems: 0
          title: Objects
          type: array
      required:
      - camera_id
      - stale
      - objects
      title: StaleLiveObjects
      type: object
    PublicLiveObjects:
      oneOf:
      - $ref: '#/components/schemas/FreshLiveObjects'
      - $ref: '#/components/schemas/StaleLiveObjects'
      description: Data get_live_objects의 최신/만료 응답. stale에 따라 필드가 다르다.
    PushStatus:
      type: object
      properties:
        enabled:
          type: boolean
        provider:
          const: fcm
          type: string
      required:
      - enabled
      - provider
    DeviceRegistrationResponse:
      type: object
      properties:
        device_id:
          type: string
          pattern: ^[a-f0-9]{32}$
          title: Device Id
        enabled:
          type: boolean
          title: Enabled
        event_types:
          anyOf:
          - items:
              type: string
            type: array
            maxItems: 100
          - type: 'null'
          title: Event Types
        platform:
          type: string
          enum:
          - android
          - ios
          title: Platform
      required:
      - device_id
      - enabled
      - event_types
      - platform
    DetailError:
      type: object
      properties:
        detail:
          type: string
      required:
      - detail
    ControlError:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            details:
              type: object
              additionalProperties: true
          required:
          - code
          - message
          - details
      required:
      - error
  securitySchemes:
    HTTPBearer:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: access_token. refresh_token을 Bearer로 사용하지 않는다.
    AccessCookie:
      type: apiKey
      in: cookie
      name: ai_cctv_access
      description: 기본 access 쿠키명. 배포의 ACCESS_COOKIE_NAME으로 변경할 수 있다.
  responses:
    ApiError:
      description: 요청·권한·상태·상위 서비스 오류. 일반 detail 또는 제어 error 구조이며 422는 detail 배열이다. 프록시나 처리하지 못한 서버 오류는
        JSON이 아닐 수 있다.
      content:
        application/json:
          schema:
            anyOf:
            - $ref: '#/components/schemas/DetailError'
            - $ref: '#/components/schemas/ControlError'
            - $ref: '#/components/schemas/HTTPValidationError'
        text/plain:
          schema:
            type: string
        text/html:
          schema:
            type: string
    Unauthorized:
      description: 인증 정보가 없거나 만료·철회·변경되었다. 현재 refresh로 한 번만 갱신하고 실패하면 다시 로그인한다.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/DetailError'
servers:
- url: /
  description: 배포된 중앙 HTTPS origin. /api/v1은 각 path에 포함된다.
x-generation:
  command: python server/services/external/tools/export_openapi.py
  check: python server/services/external/tools/export_openapi.py --check
  source: server/services/external/app/main.py:create_app().openapi()
